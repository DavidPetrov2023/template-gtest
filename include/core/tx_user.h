#ifndef TX_USER_H
#define TX_USER_H

/* ThreadX tick rate (match SysTick config) */
#define TX_TIMER_TICKS_PER_SECOND 1000

/* Optional: enable event flags, semaphores, mutexes, etc. */
// #define TX_DISABLE_ERROR_CHECKING
// #define TX_ENABLE_EVENT_TRACE

#endif
